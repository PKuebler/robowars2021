#!/usr/bin/env python3

import pygame

def handleEvents(event, playerTurn, moveMode):
    #Mausklick auswerten
    if pygame.MOUSEBUTTONUP:
        pass
    #Tastatur auswerten
    elif pygame.KEYUP:
        if moveMode:

    return playerTurn, modeMode